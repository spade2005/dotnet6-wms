namespace mvc_andy.Models.com;

public enum AuthType
{
    Menu = 1,
    Data
};
public enum StatusType
{
    Enable = 0,
    Disable
}

public enum DeleteType
{
    Enable = 0,
    Disable,
    Pending
}